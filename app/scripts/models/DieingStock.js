/*global define*/

define([
    'underscore',
    'backbone'
], function (_, Backbone) {
    'use strict';

    var DieingstockModel = Backbone.Model.extend({
        defaults: {
        }
    });

    return DieingstockModel;
});